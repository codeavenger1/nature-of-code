// array to keep track of how often random numbers are picked
let randomCounts = [];
let total = 20;

function setup() {
  createCanvas(400, 400);
  for(let i = 0; i < total; i++) {
    randomCounts[i] = 0;
  }
}

function draw() {
  background(220);
  let index = floor()
}
